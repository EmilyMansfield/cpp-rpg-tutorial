% Building this then running `pdftocairo` from `poppler` was easier than
% messing around with MathJax or anything like that.
\documentclass[preview]{standalone}

\usepackage{amsmath}

\begin{document}

\[
    \left\lfloor
        1 + k(1 + (n\ \operatorname{mod}\ 2))\tanh\left(\frac{n}{30}\right)
    \right\rfloor
\]

\end{document}
